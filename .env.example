# Copy to .env (gitignored) or set in Render/Docker environment.
# NEVER commit real API keys.
# On Render: Environment → add keys BELOW. Value must NOT include wrapping quotes.

AI_PROVIDER=openrouter
AI_BASE_URL=https://openrouter.ai/api/v1
AI_MODEL=deepseek/deepseek-chat
AI_API_KEY=
